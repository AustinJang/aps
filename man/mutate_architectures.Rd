% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{mutate_architectures}
\alias{mutate_architectures}
\title{Mutate Architectures (Genetic Algorithm)}
\usage{
mutate_architectures(parents, n, mutation_rate = 0.3, config = NULL)
}
\arguments{
\item{parents}{Data frame of parent architectures to mutate}

\item{n}{Number of mutated children to generate}

\item{mutation_rate}{Probability of mutating each parameter (default 0.3)}

\item{config}{Optional list specifying architecture parameter ranges (same format
as \code{generate_architectures}). Used to bound mutations.}
}
\value{
A data frame with mutated architecture specifications
}
\description{
Creates mutations of parent architectures by randomly perturbing parameters.
Used for genetic algorithm-style architecture search where good-performing
architectures are mutated to create the next generation.
}
\details{
Mutations include:
\itemize{
\item depth: +1 or -1 (bounded by config)
\item width: multiply by 2 or divide by 2 (bounded by config)
\item activation: randomly swap to a different function from config
\item reg/dropout: small random perturbation (bounded by config)
}
}
